name: build 8188eu driver

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install deps
        run: |
          set -eux
          sudo apt-get update
          sudo apt-get install -y \
            git make gcc-aarch64-linux-gnu bc flex bison \
            libssl-dev libelf-dev python3 python3-pip cpio rsync kmod

      - name: Clone ACK kernel source (android12-5.10-lts)
        run: |
          set -eux
          git clone --depth 1 \
            -b android12-5.10-lts \
            https://android.googlesource.com/kernel/common \
            kernel_common
          cd kernel_common
          echo "=== HEAD commit ==="
          git log --oneline -1
          echo "=== Makefile version ==="
          head -5 Makefile

      - name: Prepare kernel headers
        run: |
          set -eux
          cd kernel_common
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- gki_defconfig
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- modules_prepare
          echo "=== Module.symvers check ==="
          ls -la Module.symvers || echo "WARNING: Module.symvers not found"
          wc -l Module.symvers 2>/dev/null || true
          echo "=== kernel.release ==="
          cat include/config/kernel.release || true

      - name: Clone rtl8188eus driver (d3vilh fork)
        run: |
          set -eux
          git clone --depth 1 \
            https://github.com/d3vilh/rtl8188eus \
            driver
          cd driver
          echo "=== driver HEAD ==="
          git log --oneline -1

      - name: Patch driver Makefile for ARM64
        run: |
          set -eux
          cd driver
          sed -i 's/^CONFIG_PLATFORM_I386_PC = y/CONFIG_PLATFORM_I386_PC = n/' Makefile
          sed -i 's/^CONFIG_PLATFORM_ARM64 = n/CONFIG_PLATFORM_ARM64 = y/' Makefile
          echo "=== platform config ==="
          grep -n "CONFIG_PLATFORM" Makefile | head -30

      - name: Inspect driver Makefile
        run: |
          set -eux
          cd driver
          echo "=== modules target ==="
          grep -n -A15 "^modules:" Makefile || true
          echo "=== all/default targets ==="
          grep -n "^all:\|^default:" Makefile || true
          echo "=== KERNELDIR / KSRC refs ==="
          grep -n "KERNELDIR\|KSRC\|KERNELRELEASE" Makefile | head -30
          echo "=== main .ko target ==="
          grep -n "8188eu.ko\|\.ko:" Makefile | head -20

      - name: Build driver module (two-pass)
        run: |
          set -eux
          cd driver
          echo "=== PASS 1: generate Module.symvers ==="
          make \
            ARCH=arm64 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            -C $GITHUB_WORKSPACE/kernel_common \
            M=$PWD \
            KCFLAGS="-Wno-error=misleading-indentation -Wno-error" \
            modules 2>&1 | tee build.log || true

          echo "=== PASS 2: force module compile ==="
          make \
            ARCH=arm64 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            -C $GITHUB_WORKSPACE/kernel_common \
            M=$PWD \
            KERNELRELEASE=5.10.0 \
            KCFLAGS="-Wno-error=misleading-indentation -Wno-error" \
            modules 2>&1 | tee -a build.log || true

          echo "=== PASS 3: direct .ko target ==="
          make \
            ARCH=arm64 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            -C $GITHUB_WORKSPACE/kernel_common \
            M=$PWD \
            KERNELRELEASE=5.10.0 \
            KCFLAGS="-Wno-error=misleading-indentation -Wno-error" \
            CONFIG_RTL8188EU=m \
            8188eu.ko 2>&1 | tee -a build.log || true

          echo "=== find all .ko files ==="
          find . -name "*.ko" -type f
          ls -la *.ko 2>/dev/null || echo "no .ko in root"

      - name: Locate built .ko
        run: |
          set -eux
          echo "=== all .ko files anywhere ==="
          find driver -name "*.ko" -type f
          mkdir -p out
          find driver -name "8188eu.ko" -exec cp {} out/ \;
          if [ -z "$(ls -A out 2>/dev/null)" ]; then
            echo "8188eu.ko not found, copying all .ko"
            find driver -name "*.ko" -exec cp {} out/ \;
          fi
          if [ -z "$(ls -A out 2>/dev/null)" ]; then
            echo "NO .ko files found at all"
          fi
          echo "=== out dir ==="
          ls -la out/

      - name: Show modinfo
        run: |
          set -eux
          for f in out/*.ko; do
            echo "=== $f ==="
            modinfo "$f" || true
          done

      - name: Capture Module.symvers and metadata
        if: always()
        run: |
          set -eux
          mkdir -p out/meta
          if [ -f kernel_common/Module.symvers ]; then
            cp kernel_common/Module.symvers out/meta/Module.symvers
            echo "Module.symvers lines: $(wc -l < out/meta/Module.symvers)"
          fi
          if [ -f kernel_common/include/config/kernel.release ]; then
            cp kernel_common/include/config/kernel.release out/meta/kernel.release
            cat out/meta/kernel.release
          fi
          cd kernel_common && git log --oneline -1 > ../out/meta/kernel-commit.txt

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rtl8188eus-ko
          path: |
            out/**/*.ko
            out/meta/**
            driver/build.log
          if-no-files-found: warn
